version: '3.3'
services:
  test_db:
    image: mysql:5.6
    environment:
      MYSQL_ROOT_PASSWORD: secret
      MYSQL_DATABASE: test_db
    ports:
      - "3315:3306"
    volumes:
      - ./docker/mysql/test_db/initdb.d:/docker-entrypoint-initdb.d