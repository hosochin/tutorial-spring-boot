spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3315/sample01
    username: root
    password: secret
  jpa:
    database: MYSQL
    show-sql: true
    hibernate:
      ddl-auto: update
  data:
    rest:
      base-path: /api
